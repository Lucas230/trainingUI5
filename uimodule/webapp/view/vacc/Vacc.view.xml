 <mvc:View controllerName="com.lp.trainingUI5.controller.vacc.Vacc"
  displayBlock="true"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:unified="sap.ui.unified" >

  <Page id="vacc.Vacc" title="Vaccination Tracker" showNavButton="true" navButtonPress="onNavBack">

    <subHeader>
      <Toolbar>
        <ToolbarSpacer/>
              <RadioButton text="Table"  selected="{view>/table}" valueState="Success" groupName="Group1" />
              <RadioButton text="Calendar" selected="{view>/calendar}" valueState="Warning" groupName="Group1" />
        <ToolbarSpacer/>
      </Toolbar>
    </subHeader>

    <Table id="idVaccTable"
        items="{vacc>/}"
        visible="{view>/table}"
      >
      <columns>
        <Column
          width="12em">
          <Text text="Vaccination Name"/>
        </Column>
        <Column
          minScreenWidth="Tablet"
          demandPopin="true">
          <Text text="Company" />
        </Column>
        <Column
          minScreenWidth="Desktop"
          demandPopin="true"
          hAlign="End">
          <Text text="Date Given/To Be Given" />
        </Column>
        <Column
          minScreenWidth="Desktop"
          demandPopin="true"
          hAlign="End">
          <Text text="Type of Vaccination" />
        </Column>
      </columns>
      <items>
        <ColumnListItem vAlign="Middle">
          <cells>
            <Text
              text="{vacc>vacc}" />
            <Text
              text="{vacc>company}" />
            <ObjectNumber
              number="{vacc>date}"
              unit="{WeightUnit}"
              state="{
                path:'vacc>date',
                formatter:'.formatState'
              }"
              />
            <Text
                text="{vacc>type}" />
          </cells>
        </ColumnListItem>
      </items>
    </Table>

    <VBox class="sapUiSmallMargin" visible="{view>/calendar}">
      <SinglePlanningCalendar
        id="SPC1"
        class="sapUiSmallMarginTop"
        title= "My Calendar"
        viewChange="handleViewChange"
        appointments="{path: 'vacc>/'}">
        <views>
          <SinglePlanningCalendarDayView key="DayView" title="Day" />
          <SinglePlanningCalendarWorkWeekView key="WorkWeekView" title="Work Week" />
          <SinglePlanningCalendarWeekView key="WeekView" title="Week" />
          <SinglePlanningCalendarMonthView key="MonthView" title="Month" />
        </views>
        <appointments>
          <unified:CalendarAppointment
            title= "{vacc>vacc}"
            text= "{vacc>company}"
            type="{vacc>ctype}"
            startDate= "{path: 'vacc>date', formatter: '.formatDate'}"
            endDate= "{path: 'vacc>date', formatter: '.formatDate'}">
          </unified:CalendarAppointment>
        </appointments>
      </SinglePlanningCalendar>
    </VBox>
  </Page>
</mvc:View>